#include <stdio.h>

void even_odd(int n)
{
    if (n % 2 == 0)
    {
        printf("%d is even.\n", n);
    }
    else
    {
        printf("%d is odd.\n", n);
    }
}

int main()
{
    int n;

    scanf("%d", &n);

    even_odd(n);

    return 0;
}


